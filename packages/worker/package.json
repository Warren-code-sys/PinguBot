{
  "name": "@pingu/worker",
  "version": "1.0.0",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "dependencies": {
    "bullmq": "^5.5.0",
    "ioredis": "^5.3.2",
    "ioredis-mock": "^5.7.0",
    "@pingu/core": "workspace:*"
  },
  "scripts": {
    "build": "tsup src/index.ts --format esm,cjs --dts",
    "dev": "tsx watch src/index.ts",
    "test": "vitest"
  }
}
