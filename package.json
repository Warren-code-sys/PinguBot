{
  "name": "pingu-trade-monitor",
  "private": true,
  "packageManager": "pnpm@10.11.0",
  "scripts": {
    "dev": "docker compose -f infra/docker-compose.yml up -d && pnpm prisma migrate deploy && pnpm -r --parallel dev",
    "build": "pnpm -r build",
    "lint": "eslint .",
    "test": "pnpm -r --if-present test",
    "mock:pingu": "tsx scripts/mock-pingu.ts"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "tsx": "^4.7.0",
    "tsup": "^7.2.0",
    "eslint": "^8.57.0",
    "eslint-config-prettier": "^9.1.0",
    "vitest": "^1.5.0",
    "supertest": "^6.3.3"
  }
}
